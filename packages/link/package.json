{
  "name": "@beta/link",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf tsconfig.tsbuildinfo dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
