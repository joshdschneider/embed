<!doctype html>
<html>
  <head>
    <title>Error</title>
    <style>
      html,
      body,
      main {
        width: 100%;
        height: 100%;
        margin: 0;
        padding: 0;
      }
      * {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI (Custom)', Roboto,
          'Helvetica Neue', 'Open Sans (Custom)', system-ui, sans-serif, 'Apple Color Emoji',
          'Segoe UI Emoji';
        --background-color: <%= '#' + branding.page_background %>;
        --border-color: <%= prefers_dark_mode ? 'rgba(255, 255, 255, .1)': 'rgba(0, 0, 0, .1)' %>;
        --text-color: <%= prefers_dark_mode ? 'rgba(255, 255, 255, 1)': 'rgba(28, 32, 36, 1)' %>;
        --border-radius: var(--border-radius-<%= branding.border_radius %>);
        --border-radius-none: 0px;
        --border-radius-small: 3px;
        --border-radius-medium: 6px;
        --border-radius-large: 12px;
        --border-radius-full: 18px;
      }
      main {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100%;
        min-height: 500px;
        background-color: var(--background-color);
      }
      .container {
        display: flex;
        flex-direction: column;
        flex-grow: 1;
        text-align: center;
        width: calc(100% - 40px);
        height: calc(100% - 40px);
        max-width: 400px;
        max-height: 445px;
        margin: 20px;
      }
      .icon {
        width: 40px;
        height: 40px;
        padding: 9px;
        border-radius: 50%;
        object-fit: cover;
        background: var(--background-color);
        border: 1px solid var(--border-color);
      }
      .icon img {
        width: 100%;
        height: 100%;
      }
    </style>
  </head>
  <body>
    <main>
      <div class="container">
        <div class="icon">
          <img src="" alt="Error" />
        </div>
        <h1>Error</h1>
        <p><%= message || "Something went wrong" %></p>
      </div>
    </main>
  </body>
</html>
